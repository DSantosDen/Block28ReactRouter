/* Red is a functional component that only contains a render method and doesn't have it's own state. */

function Red() {
  return (
    <div className="redpage">
      <h1>RED</h1>
    </div>
  );
}

export default Red;
